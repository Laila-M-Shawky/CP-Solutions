// #include <bits/stdc++.h>// using namespace std;// #define fast_io ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);// #define ll long long // ll testcases = 1; // void solve()// {//     int quesMarks = 0, counter;//     string s, t;//     cin >> s >> t;//     if (s == t)//     {//         cout << "YES\n" << s << endl;//         return;//     }//     for (auto &it : s)//     {//         if (it == '?')//         {//             quesMarks++;//         }//     }//     if (!quesMarks)//     {//         counter = 0;//         if (s.find(t) == string::npos)//             cout << "NO" << endl;//         else//             cout << "YES" << endl << s << endl;//         return;//     }//     else if (quesMarks >= t.size())//     {//         counter = 0;//         cout << "YES\n";//         for (int i = 0; i < s.size(); ++i)//         {//             if (s[i] == '?')//             {//                 s[i] = t[(counter++) % int(t.size())];//             }//         }//         cout << s << endl;//         return;//     }//     else if (quesMarks < t.size())//     {//         vector<ll> indexes;//         quesMarks = counter = 0;//         for (int i = 0; i < s.size(); ++i)//         {//             if (s[i] == t[counter + quesMarks])//                 counter++;//             else if (s[i] == '?')//                 indexes.push_back(counter + quesMarks++);//         }//         if (counter + quesMarks != t.size())//         {//             cout << "NO" << endl;//             return;//         }//         counter = 0;//         for (int i = 0; i < s.size(); ++i)//         {//             if (s[i] == '?')//             {//                 s[i] = t[indexes[counter++]];//             }//         }//         cout << "YES\n"//              << s << endl;//     }//     return;// } // int main()// {//     fast_io//             cin >>//         testcases;//     while (testcases--)//     {//         solve();//     }//     return 0;// } #include <bits/stdc++.h>using namespace std;#define fast_io ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);#define ll long long ll testcases = 1; void solve(){    string s, t;    cin >> s >> t;     int n = (int)s.size();    int m = (int)t.size();    int j = 0;                  // مؤشر على t     // حاول نحقق t كـ subsequence ونملأ '?' حسب الحاجة    for (int i = 0; i < n; ++i)    {        if (j < m && (s[i] == '?' || s[i] == t[j]))        {            if (s[i] == '?') s[i] = t[j];            ++j; // تمّت مطابقة حرف من t        }    }     if (j < m)    {        cout << "NO\n";        return;    }     // اكمل ملء أي '?' متبقية بأي حرف (مثلاً 'a')    for (int i = 0; i < n; ++i)        if (s[i] == '?') s[i] = 'a';     cout << "YES\n" << s << "\n";} int main(){    fast_io    cin >> testcases;    while (testcases--)    {        solve();    }    return 0;}